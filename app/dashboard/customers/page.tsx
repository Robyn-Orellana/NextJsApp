import { Metadata } from 'next';
 
export const metadata: Metadata = {
  title: 'Customer',
};

export default function Page(){
    return <p>Customemrs Page, este es el texto de la pagina customers</p>;
}